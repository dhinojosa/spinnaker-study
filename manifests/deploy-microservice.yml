kind: Deployment
apiVersion: apps/v1
metadata:
    labels:
        app: drobles-solo
        stack: frontend
        source: demo
    name: drobles-solo
    namespace: microservices
spec:
    replicas: 3
    selector:
        matchLabels:
            run: drobles-solo
    template:
        metadata:
            labels:
                app: drobles-solo
                run: drobles-solo
                source: demo
        spec:
            containers:
                -   image: 219099013464.dkr.ecr.us-west-2.amazonaws.com/drobles-solo
                    name: drobles-solo
                    ports:
                        -   name: web
                            containerPort: 8080
---

kind: Service
apiVersion: v1
metadata:
    labels:
        app: drobles-solo-service
    name: drobles-solo-service
    namespace: microservices
spec:
    selector:
        source: demo
        app: drobles-solo
    ports:
        -   name: web
            protocol: TCP
            port: 30201
            targetPort: 8080

---

apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
    annotations:
        nginx.ingress.kubernetes.io/rewrite-target: /$2
    name: mild-temper-ingress
    namespace: microservices
spec:
    rules:
        -   http:
                paths:
                    -   path: /mild-temper(/|$)(.*)
                        backend:
                            serviceName: drobles-solo-service
                            servicePort: 30201


