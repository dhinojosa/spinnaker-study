kind: Deployment
apiVersion: apps/v1
metadata:
    labels:
        app: arbitrary-oppertunity-microservice
        stack: frontend
        source: demo
    name: arbitrary-oppertunity-microservice
    namespace: microservices
spec:
    replicas: 3
    selector:
        matchLabels:
            run: arbitrary-oppertunity-microservice
    template:
        metadata:
            labels:
                app: arbitrary-oppertunity-microservice
                run: arbitrary-oppertunity-microservice
                source: demo
        spec:
            containers:
                -   image: 219099013464.dkr.ecr.us-west-2.amazonaws.com/arbitrary-oppertunity-microservice
                    name: arbitrary-oppertunity-microservice
                    ports:
                        -   name: web
                            containerPort: 8080
---

kind: Service
apiVersion: v1
metadata:
    labels:
        app: arbitrary-oppertunity-microservice-service
    name: arbitrary-oppertunity-microservice-service
    namespace: microservices
spec:
    selector:
        source: demo
        app: arbitrary-oppertunity-microservice
    ports:
        -   name: web
            protocol: TCP
            port: 30201
            targetPort: 8080

---

apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
    annotations:
        nginx.ingress.kubernetes.io/rewrite-target: /$2
    name: arbitrary-oppertunity-ingress
    namespace: microservices
spec:
    rules:
        -   http:
                paths:
                    -   path: /arbitrary-oppertunity(/|$)(.*)
                        backend:
                            serviceName: arbitrary-oppertunity-microservice-service
                            servicePort: 30201
