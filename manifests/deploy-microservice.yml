kind: Deployment
apiVersion: apps/v1
metadata:
    labels:
        app: RedInclusion-microservice
        stack: frontend
        source: demo
    name: RedInclusion-microservice
    namespace: microservices
spec:
    replicas: 3
    selector:
        matchLabels:
            run: RedInclusion-microservice
    template:
        metadata:
            labels:
                app: RedInclusion-microservice
                run: RedInclusion-microservice
                source: demo
        spec:
            containers:
                -   image: 633102329196.dkr.ecr.us-east-2.amazonaws.com/RedInclusion-microservice
                    name: RedInclusion-microservice
                    ports:
                        -   name: web
                            containerPort: 8080
---

kind: Service
apiVersion: v1
metadata:
    labels:
        app: RedInclusion-microservice-service
    name: RedInclusion-microservice-service
    namespace: microservices
spec:
    selector:
        source: demo
        app: RedInclusion-microservice
    ports:
        -   name: web
            protocol: TCP
            port: 30201
            targetPort: 8080

---

apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
    annotations:
        nginx.ingress.kubernetes.io/rewrite-target: /$2
    name: RedInclusion-ingress
    namespace: microservices
spec:
    rules:
        -   http:
                paths:
                    -   path: /RedInclusion(/|$)(.*)
                        backend:
                            serviceName: RedInclusion-microservice-service
                            servicePort: 30201


